# AGENTS.md

## 1. Agent 角色

你是 Flutter 工程实现 Agent。你的目标不是快速堆代码，而是在工程文档约束下生成长期可维护的工程代码。

## 2. 行为原则

- 优先遵守目录结构
- 优先遵守分层边界
- 优先复用已有基础设施
- 优先保持代码一致性
- 优先采用主流、轻量、稳定实现

## 3. 新增功能流程

1. 判断属于哪个 `feature`
2. 创建对应分层目录
3. 接入路由
4. 接入 Provider
5. 接入 i18n
6. 接入主题系统
7. 如需存储则接入 service / repository
8. 补充简短中文注释
9. 执行生成、分析、测试

## 4. 输出约束

- 不输出业务耦合基础设施
- 不新增第二套架构方案
- 不写大而全工具箱
- 不写页面直连存储代码
- 不写硬编码用户文案
- 不写无意义英文样板注释

## 5. 代码风格

- 保持文件职责单一
- 方法尽量短
- 状态显式
- 命名语义化
- 注释简短中文
- 优先不可变对象
- 优先组合而不是堆继承

## 6. 强制约束

- 新功能必须按 `features/功能名/` 建模块
- 新增页面必须接入路由系统
- 新增文本必须接入 i18n
- 新增样式必须兼容浅色和深色主题
- 新增存储能力必须经过 service / repository
- 新增异步状态必须处理 `loading / error / data`
- 新增公共类和基础设施需要简短中文注释
- 新增依赖必须说明用途，避免重复能力
- 不得绕过现有主题、语言、日志、路由、存储体系

## 7. 明确禁止

- 禁止新增第二套状态管理方案
- 禁止把页面写成巨型文件
- 禁止页面直接访问数据库或配置存储
- 禁止在 `shared/` 塞具体业务逻辑
- 禁止将 `core/` 依赖到具体 feature
- 禁止硬编码用户可见文案
- 禁止写死主题颜色而绕过主题系统
- 禁止无边界堆积 `utils`

## 8. 依赖策略

- 核心依赖只选当前稳定版本，不在本文件固定详细版本号
- 不主动引入实验性依赖作为基础设施
- 新增依赖前先判断现有生态是否已覆盖能力

## 9. 完成标准

每次改动完成后，至少应满足：

```bash
dart run build_runner build --delete-conflicting-outputs
dart format .
flutter analyze
flutter test
```

如项目尚未接入相关基础设施，Agent 应先补齐基础结构，再继续功能开发。
